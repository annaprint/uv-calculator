# /etc/nginx/conf.d/uv-calc-limits.conf
# limit_req_zone is only valid at the http{} level; vhost in
# sites-available/calc.citi-print.ru references it via limit_req.

limit_req_zone $binary_remote_addr zone=login_zone:10m rate=10r/m;
